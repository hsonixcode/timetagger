# TimeTagger Environment Example
# Copy this file to .env and modify as needed

# PostgreSQL Database Configuration
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=your_database_name
POSTGRES_HOST=postgres
POSTGRES_PORT=5432  # This remains 5432 inside the container

# TimeTagger Database Connection
# Using PostgreSQL for multi-user support
TIMETAGGER_DB_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
# Or use this format for SQLite (simpler for single-user setups)
# TIMETAGGER_DB_URL=sqlite:///app/data/timetagger.db

# TimeTagger Server Configuration
TIMETAGGER_BIND=0.0.0.0:8000  # Bind to all interfaces on port 8000
TIMETAGGER_LOG_LEVEL=info     # Options: debug, info, warning, error
TIMETAGGER_DATA_DIR=/app/data
TIMETAGGER_CREDENTIALS=your_username:your_password  # Plain text credentials

# Azure authentication is configured through the External Auth UI
# Configure via Settings > Configure External Auth in the web UI 